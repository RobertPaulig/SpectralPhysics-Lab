# SpectralPhysics-Lab

**SpectralPhysics-Lab** — это экспериментальный кодовый стенд для новой "спектральной физики":

- Вместо шариков и сил: **Среда** и **колебательные режимы** (моды).
- Вместо "масса тянет массу": **спектральная тень** и **давление фонового поля**.
- Вместо магии квантовой механики: **устойчивые резонаторы** (солитоны) в единой Среде.

Цель репозитория — собрать **рабочий набор инструментов**, с которым можно:

- моделировать простейшие "среды" и солитоны;
- считать спектры (частотные распределения, локальную плотность состояний);
- играться с "маской" (эффективная масса, гравитация, валентность) как с **спектральными объектами**;
- готовить основу для прикладных задач (диагностика машин, геофизика, т.д.).

> Важно: это не "красивая теория на бумаге", а песочница,
> где любая идея должна доползти до **работающего кода / эксперимента**.

---

## Installation

```bash
pip install -e .
spectral-health --help
```

---

## Quick start: Pump health demo

```bash
# 1. Сгенерировать синтетические данные
python examples/generate_synthetic_pump_data.py

# 2. Обучить профиль "здорового" состояния
spectral-health train \
  --config configs/pump_train.yaml \
  --out data/pump/profile.npz

# 3. Оценить текущее состояние и получить отчёт
spectral-health score \
  --config configs/pump_score.yaml \
  --profile data/pump/profile.npz \
  --thresholds configs/pump_thresholds.yaml \
  --report data/pump/report.md
```

## 2D medium demo

Смотрите `examples/medium_2d_modes_demo.ipynb` — там видно,
как у пластины появляются моды колебаний (стоячие волны).

## Atomic valence as surface resonances (toy model)

Смотрите `examples/atomic_valence_demo.ipynb`: там показано,
как можно думать про валентность как про количество совпадающих
резонансных частот между "атомами"-резонаторами.

---

## Текущий технический фокус

1. **Базовая математика**  
   - Реализация *симметричного метода Ньютона* для поиска корней нелинейных уравнений и систем **без явных производных**.
   - Обёртки для устойчивого решения задач "подбор параметров модели под данные".

2. **Спектральные представления**
   - Классы для описания 1D-спектров (частоты, амплитуды, фазы).
   - Простейшая модель *локальной спектральной плотности* `rho_spec(x, ω)` в дискретном виде (сетка по x и по частоте).

3. **Простые физические игрушки (toy models)**
   - 1D-цепочка осцилляторов (упругая Среда).
   - Спектральный анализ нормальных мод (моды колебаний цепочки).
   - Простейшая "гравитационная" модель как **дисбаланс спектра**: влияние "прозрачности" материала на эффективное "ускорение".

---

## Планируемая структура репозитория

```text
SpectralPhysics-Lab/
├─ README.md                # этот файл
├─ AI_TASKS.md              # задания для ИИ-ассистента
├─ pyproject.toml           # упаковка проекта как библиотеки
├─ src/
│  └─ spectral_physics/
│     ├─ __init__.py
│     ├─ root_finding.py    # симметричный Ньютон и обёртки
│     ├─ spectrum.py        # классы спектров и спектральных операций
│     ├─ cli.py             # CLI spectral-health
│     └─ ...
```
