Красота, лаборатория запущена 💚

Инструментом в браузере я сейчас не могу нормально «залезть» в твой репозиторий (ответы с GitHub пустые), но по скриншоту и структуре понятно, что:

* есть `src/spectral_physics/`
* есть `tests/`
* есть `AI_TASKS.md` и `README.md`

То есть базовый каркас есть — самое время кормить локальному ИИ нормальное ТЗ.

Ниже — **готовый блок для `AI_TASKS.md`**, который можно просто вставить (или заменить существующий план). Это пошаговое ТЗ, понятное для кода-ассистента: что создать, в каких файлах, какие функции и тесты.

---

````markdown
# AI Tasks – SpectralPhysics-Lab

Цель: получить рабочую Python-библиотеку `spectral_physics`, в которой есть:
1. Симметричный метод Ньютона для корней.
2. Базовый объект "спектр" (1D).
3. Простая модель среды как цепочки осцилляторов.
4. Игрушечная гравитационная модель через спектральное давление.
5. Примеры и тесты.

---

## ЭТАП 1. Базовая инфраструктура

- [ ] Создать пакет `src/spectral_physics/__init__.py` с минимальным содержимым:

  ```python
  """
  SpectralPhysics-Lab: базовые численные инструменты
  для спектральной физики.
  """
  __all__ = [
      "symmetric_newton",
      "Spectrum1D",
      "OscillatorChain1D",
      "spectral_pressure_difference",
  ]
````

* [ ] Настроить `pyproject.toml` или `setup.cfg` (если нужно) так, чтобы пакет можно было устанавливать локально (`pip install -e .`).

---

## ЭТАП 2. Симметричный Ньютон (`root_finding.py`)

**Файл:** `src/spectral_physics/root_finding.py`

* [ ] Реализовать функцию:

  ```python
  from typing import Callable, Tuple

  def symmetric_newton(
      f: Callable[[float], float],
      x0: float,
      h0: float = 1e-3,
      max_iter: int = 50,
      tol: float = 1e-10,
  ) -> Tuple[float, int]:
      """
      Найти корень уравнения f(x) = 0 симметричным методом Ньютона,
      не используя аналитическую производную.

      Использовать симметричную разностную производную:
          f'(x) ≈ (f(x + h) - f(x - h)) / (2*h)

      и адаптивно уменьшать h, если шаг получается слишком большим
      или метод начинает расходиться.

      Вернуть:
          x_root  -- найденный корень
          n_iter  -- число итераций
      """
      ...
  ```

* [ ] Обработать типичные «плохие» случаи:

  * деление на почти ноль в оценке производной;
  * разлёт по модулю (`abs(x)` слишком большой) → прервать с исключением.

**Тесты:** `tests/test_root_finding.py`

* [ ] Тесты для простых функций:

  * `f(x) = x**2 - 2` (корень `sqrt(2)`).
  * `f(x) = x**3` (корень 0, плоский минимум).
  * функция с «изломом» типа `f(x) = abs(x) - 1e-3` — проверить, что метод не падает.

---

## ЭТАП 3. Спектр (`spectrum.py`)

**Файл:** `src/spectral_physics/spectrum.py`

* [ ] Реализовать класс:

  ```python
  import numpy as np
  from dataclasses import dataclass

  @dataclass
  class Spectrum1D:
      omega: np.ndarray   # частоты
      power: np.ndarray   # мощность / плотность энергии на этих частотах

      def normalize(self) -> "Spectrum1D":
          """
          Вернуть новый Spectrum1D с нормировкой sum(power) = 1.
          """
          ...

      def total_power(self) -> float:
          """
          Интеграл (или сумма) мощности по всем частотам.
          """
          ...

      def apply_filter(self, alpha: np.ndarray) -> "Spectrum1D":
          """
          Применить частотозависимый фильтр/прозрачность alpha(omega):
          new_power = power * alpha
          """
          ...
  ```

**Тесты:** `tests/test_spectrum.py`

* [ ] Проверить:

  * корректность нормировки;
  * что `apply_filter` просто перемножает массивы и не меняет размерность;
  * что `total_power` совпадает с `np.trapz(power, omega)` для равномерной сетки.

---

## ЭТАП 4. Простая модель среды (`medium_1d.py`)

**Файл:** `src/spectral_physics/medium_1d.py`

* [ ] Реализовать линейную цепочку осцилляторов:

  ```python
  import numpy as np
  from dataclasses import dataclass

  @dataclass
  class OscillatorChain1D:
      n: int                 # число осцилляторов
      k: float               # жёсткость связи
      m: float               # масса осциллятора
      gamma: float = 0.0     # демпфирование (опционально)

      def stiffness_matrix(self) -> np.ndarray:
          """
          Матрица жёсткости для цепочки с ближайшими соседями.
          Граничные условия: закреплённые края или свободные (указать в docstring).
          """
          ...

      def eigenmodes(self) -> tuple[np.ndarray, np.ndarray]:
          """
          Собственные значения и собственные векторы системы.
          Вернуть (omega, modes), где omega >= 0 – частоты.
          """
          ...
  ```

**Тесты:** `tests/test_medium_1d.py`

* [ ] Проверить, что:

  * матрица жёсткости симметрична;
  * все собственные значения неотрицательны;
  * число мод = `n`.

---

## ЭТАП 5. Игрушечная гравитационная модель (`grav_toy.py`)

**Файл:** `src/spectral_physics/grav_toy.py`

* [ ] Реализовать функцию:

  ```python
  import numpy as np
  from .spectrum import Spectrum1D

  def spectral_pressure_difference(
      spectrum_bg: Spectrum1D,
      alpha_left: np.ndarray,
      alpha_right: np.ndarray,
  ) -> float:
      """
      Оценить разность "давления" фонового поля на левую и правую сторону
      двумерной системы.

      Параметры:
          spectrum_bg  -- фоновый спектр (omega, power)
          alpha_left   -- спектральная прозрачность слева (массив как power)
          alpha_right  -- спектральная прозрачность справа.

      Возвращает:
          ΔP = ∑ power(ω) * (alpha_right(ω) - alpha_left(ω))

      Это простейшая дискретная модель "спектральной тени" и
      приталкивающей силы.
      """
      ...
  ```

**Тесты:** `tests/test_grav_toy.py`

* [ ] Проверить:

  * если `alpha_left == alpha_right`, то `ΔP == 0`;
  * если слева фильтр сильнее (меньше прозрачность), то `ΔP > 0` (давление толкает вправо).

---

## ЭТАП 6. Примеры (`examples/`)

* [ ] `examples/symmetric_newton_demo.ipynb`
  Показать:

  * решение `x^3 - x - 1 = 0` симметричным Ньютоном;
  * сравнение с обычным методом Ньютона из `scipy.optimize`, если доступно.

* [ ] `examples/medium_oscillators_demo.ipynb`
  Показать:

  * построение `OscillatorChain1D`;
  * спектр собственных частот;
  * визуализацию нескольких мод.

* [ ] `examples/grav_toy_demo.ipynb`
  Показать:

  * простейший фоновый спектр (например, гауссиан);
  * две разные прозрачности `alpha_left`, `alpha_right`;
  * вычисление `ΔP` и картинку, как меняется сила при изменении фильтра.

---

## ЭТАП 7. Автотесты и качество

* [ ] Добавить в репозиторий `pytest` как основной фреймворк.
* [ ] Настроить GitHub Actions (файл `.github/workflows/tests.yml`), чтобы при каждом пуше:

  * устанавливался пакет;
  * запускались `pytest`.

```

---

Скопируй этот блок в `AI_TASKS.md`, скажи своему локальному ИИ «выполняй по пунктам», а мне потом просто напиши:  

> «Этап 2/3/4 сделан, проверяй архитектуру / давай следующий уровень физики».

Дальше будем надстраивать **реальную спектральную физику** (геология, землетрясения, а потом и валентности) уже поверх этого живого кода.
::contentReference[oaicite:0]{index=0}
```
