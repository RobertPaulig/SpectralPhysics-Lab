АХА, вот оно пошло в боевую фазу:
*«Сделай мне такой README, чтобы другой ИИ пахал за нас»* — это я люблю 😄

Сразу честно:
я **не могу** подключиться к GitHub или к твоей IDE и что-то там нажимать.
Но я могу дать тебе **готовые файлы**, которые ты просто:

1. Создашь в репо (`README.md`, `AI_TASKS.md`),
2. Вставишь туда текст,
3. Скажешь IDE-боту: «читай `AI_TASKS.md` и делай по шагам».

Ниже — уже **готовый контент**, который можно заливать как есть.

---

## 1. Предлагаемое имя репозитория

Например:

`SpectralPhysics-Lab`
или
`spectral-medium-engine`

(выбери сам, но я буду писать как будто это `SpectralPhysics-Lab`.)

---

## 2. Файл `README.md`

````markdown
# SpectralPhysics-Lab

**SpectralPhysics-Lab** — это экспериментальный кодовый стенд для новой "спектральной физики":

- Вместо шариков и сил: **Среда** и **колебательные режимы** (моды).
- Вместо "масса тянет массу": **спектральная тень** и **давление фонового поля**.
- Вместо магии квантовой механики: **устойчивые резонаторы** (солитоны) в единой Среде.

Цель репозитория — собрать **рабочий набор инструментов**, с которым можно:

- моделировать простейшие "среды" и солитоны;
- считать спектры (частотные распределения, локальную плотность состояний);
- играться с "маской" (эффективная масса, гравитация, валентность) как с **спектральными объектами**;
- готовить основу для прикладных задач (диагностика машин, геофизика, т.д.).

> Важно: это не "красивая теория на бумаге", а песочница,
> где любая идея должна доползти до **работающего кода / эксперимента**.

---

## Текущий технический фокус

1. **Базовая математика**  
   - Реализация *симметричного метода Ньютона* для поиска корней
     нелинейных уравнений и систем **без явных производных**.
   - Обёртки для устойчивого решения задач "подбор параметров модели под данные".

2. **Спектральные представления**
   - Классы для описания 1D-спектров (частоты, амплитуды, фазы).
   - Простейшая модель *локальной спектральной плотности* `rho_spec(x, ω)`
     в дискретном виде (сетка по x и по частоте).

3. **Простые физические игрушки (toy models)**
   - 1D-цепочка осцилляторов (упругая Среда).
   - Спектральный анализ нормальных мод (моды колебаний цепочки).
   - Простейшая "гравитационная" модель как **дисбаланс спектра**:
     влияние "прозрачности" материала на эффективное "ускорение".

---

## Язык и стек

- Язык: **Python 3.11+**
- Библиотеки:
  - `numpy`
  - `scipy`
  - `matplotlib` (для графиков)
  - (`pytest` для тестов — позже)

---

## Планируемая структура репозитория

```text
SpectralPhysics-Lab/
├─ README.md                # этот файл
├─ AI_TASKS.md              # задания для ИИ-ассистента
├─ pyproject.toml / setup.cfg (позже)  # упаковка проекта как библиотеки
├─ src/
│  └─ spectral_physics/
│     ├─ __init__.py
│     ├─ root_finding.py        # симметричный Ньютон и обёртки
│     ├─ spectrum.py            # классы спектров и спектральных операций
│     ├─ medium_1d.py           # 1D модель среды (цепочка осцилляторов)
│     ├─ grav_toy.py            # простая спектральная "гравитация"
│     └─ utils.py               # вспомогательные функции
├─ examples/
│  ├─ symmetric_newton_demo.ipynb
│  ├─ spectrum_demo.ipynb
│  └─ medium_oscillators_demo.ipynb
└─ tests/
   ├─ test_root_finding.py
   ├─ test_spectrum.py
   └─ test_medium_1d.py
````

---

## Как с этим работать (для человека)

1. Клонировать репозиторий.
2. Установить зависимости (позже будет `pip install -e .`).
3. Запускать примеры из `examples/`, чтобы:

   * понять, как работать с симметричным Ньютоном;
   * посмотреть, как устроены спектры;
   * увидеть, как простая среда рождает набор мод и как с ними можно играться.

---

## Как с этим работать (для ИИ-ассистента IDE)

В файле [`AI_TASKS.md`](./AI_TASKS.md) описаны **поэтапные задания**:

* читать секции сверху вниз;
* не перепрыгивать через шаги;
* прежде чем писать код, **прочитать и понять** спецификацию в этой секции;
* писать код, который:

  * чистый,
  * покрыт минимальными тестами,
  * снабжён докстрингами и понятными именами.

Человек (автор репозитория) будет контролировать архитектуру и смысл,
ИИ — заниматься рутинной реализацией.

---

````

---

## 3. Файл `AI_TASKS.md`

Этот файл — специально для IDE-бота. Ты создаёшь его в корне репо и говоришь машине типа:  
«Читай AI_TASKS.md, выполняй задачи по порядку».

```markdown
# AI_TASKS: план работ для ИИ-ассистента

Этот файл описывает **поэтапный план разработки**.

Общие правила для ИИ:

- Выполняй задачи **по порядку**, от 1 к N.
- Не перепрыгивай шаги. Если что-то непонятно — добавь комментарий в коде или TODO.
- Пиши код так, чтобы его мог читать живой человек:
  - говорящие имена переменных/функций,
  - короткие функции,
  - докстринги.
- Для каждой ключевой функции старайся написать хотя бы **1–2 простых теста**.

---

## ЭТАП 1. Базовая инфраструктура проекта

**Задача 1.1.**  
Создать структуру Python-проекта, как описано в `README.md`:

- каталог `src/spectral_physics/` с `__init__.py`;
- каталоги `examples/` и `tests/`.

Не добавлять сложных зависимостей, только стандартный Python + `numpy`.

---

## ЭТАП 2. Симметричный метод Ньютона (root_finding.py)

Цель: реализовать **устойчивый поиск корней** нелинейных функций без явной производной.

**Задача 2.1.**  
В файле `src/spectral_physics/root_finding.py` реализовать функцию:

```python
def symmetric_newton(
    f,
    x0,
    max_iter=50,
    tol=1e-8,
    h=1e-6,
    verbose=False,
):
    """
    Находит корень уравнения f(x) = 0, используя симметричный метод Ньютона
    с численным приближением производной.

    Параметры:
        f: вызываемая функция f(x) -> float
        x0: начальное приближение
        max_iter: максимальное число итераций
        tol: допуск по |f(x)|
        h: шаг для симметричной разности
        verbose: если True, печатать прогресс

    Возвращает:
        x_root: найденное значение x, для которого f(x) ≈ 0
    """
    ...
````

Требования:

* Использовать **симметричную разностную производную**:

  * f'(x) ≈ (f(x + h) − f(x − h)) / (2h)
* Защититься от деления на очень маленькую производную (если |f'| < eps — останавливать или менять h).
* Вернуть решение и не падать с ошибкой при типичных "плохих" функциях (типа x^(1/3)).

**Задача 2.2.**
Добавить несколько простых тестов в `tests/test_root_finding.py`:

* Корень линейной функции f(x) = x − 3 → x = 3
* Корень квадратичной f(x) = x^2 − 4 → x = ±2
* Функция с плохой производной (например, f(x) = x**(1/3)) — убедиться, что алгоритм сходится к x=0 с разумной точностью.

---

## ЭТАП 3. Спектры (spectrum.py)

Цель: иметь удобный объект, который хранит **дискретный спектр** и умеет с ним работать.

**Задача 3.1.**
Создать в `src/spectral_physics/spectrum.py` класс:

```python
import numpy as np

class Spectrum1D:
    """
    Дискретный 1D-спектр: частоты, амплитуды, фазы.

    Используется как базовый кирпич для работы со спектром колебаний Среды.
    """

    def __init__(self, freqs, amps, phases=None):
        ...
```

Требования:

* `freqs`: 1D numpy array частот (в Гц или рад/с — не важно, просто числа).
* `amps`: 1D numpy array амплитуд той же длины.
* `phases` (опционально): 1D numpy array фаз, по умолчанию нули.
* Проверять размерности и типы, выбрасывать понятные исключения.

Методы, которые нужно реализовать:

* `power()` — вернуть массив мощности (amps**2).
* `total_power()` — скаляр, сумма мощности.
* `normalize_power(target=1.0)` — нормировать спектр так, чтобы `total_power() == target`.
* `copy()` — вернуть копию спектра.

**Задача 3.2.**
В `tests/test_spectrum.py` добавить тесты:

* Создание простого спектра из 3 частот.
* Проверка `total_power`.
* Проверка работы `normalize_power`.

---

## ЭТАП 4. Простая модель Среды (medium_1d.py)

Цель: реализовать простейшую 1D-модель "Среды" как цепочки связанных осцилляторов.

**Задача 4.1.**
Создать класс `OscillatorChain1D` в `src/spectral_physics/medium_1d.py`:

```python
import numpy as np

class OscillatorChain1D:
    """
    Линейная цепочка связанных осцилляторов с ближайшими соседями.

    Используется как игрушечная модель "Среды":
    - каждая точка = масса на пружине,
    - пружины соединяют соседей.
    """

    def __init__(self, n_points, k_coupling=1.0, m=1.0):
        ...
```

Требования:

* `n_points`: число осцилляторов.
* `k_coupling`: коэффициент связи.
* `m`: масса каждой точки.
* Построить матрицу жёсткости/динамики и уметь находить собственные частоты и собственные векторы (`modes`).

Методы:

* `compute_modes()` → `(freqs, modes)`:

  * `freqs`: массив собственных частот.
  * `modes`: матрица (n_points, n_modes) — собственные векторы.

**Задача 4.2.**
В `tests/test_medium_1d.py` проверить:

* Что для цепочки из 2–3 осцилляторов частоты упорядочены по возрастанию.
* Что число мод равно числу осцилляторов.

---

## ЭТАП 5. "Гравитационная" игрушка (grav_toy.py)

Цель: реализовать **очень простую** модель идеи:

> "гравитация = дисбаланс спектра"
> (то есть эффективная сила зависит от того, как изменился спектр между двумя точками).

**Задача 5.1.**
Создать в `src/spectral_physics/grav_toy.py` функцию:

```python
from .spectrum import Spectrum1D
import numpy as np

def spectral_pressure_difference(spec_left: Spectrum1D, spec_right: Spectrum1D) -> float:
    """
    Оценка 'градиента давления' между двумя точками как разности
    их полной спектральной мощности.

    Возвращает скаляр:
        p_left - p_right
    где p = total_power().
    """
    ...
```

Пока без физического смысла — просто:

* считаем `p_left = spec_left.total_power()`,
* считаем `p_right = spec_right.total_power()`,
* возвращаем `p_left - p_right`.

Это задел для последующих моделей.

**Задача 5.2.**
Добавить простой тест:

* два спектра, у одного суммарная мощность больше — знак результата должен соответствовать этому.

---

## ЭТАП 6. Примеры (examples/)

**Задача 6.1.**
Создать `examples/symmetric_newton_demo.ipynb`:

* показать решение пары уравнений с помощью `symmetric_newton`;
* визуализировать, как меняется |f(x)| по итерациям.

**Задача 6.2.**
Создать `examples/medium_oscillators_demo.ipynb`:

* создать цепочку из N осцилляторов,
* посчитать моды,
* нарисовать несколько низших мод (формы колебаний).

---

## Дальнейшие шаги (ПОКА НЕ ВЫПОЛНЯТЬ)

Следующие задачи будут добавлены позже человеком:

* улучшенная модель "гравитации" через спектральную прозрачность;
* связь с идеями локальной спектральной плотности (rho_spec);
* прикладные сценарии (машины, геофизика и т.д.).

До тех пор сосредоточься на ЭТАПАХ 1–6.

```

---

### Что дальше делать тебе

1. Создаёшь новый репо на GitHub.
2. Добавляешь туда два файла:
   - `README.md` (вставляешь мой текст),
   - `AI_TASKS.md` (вставляешь мой текст).
3. Открываешь свою IDE с ИИ (Cursor, Windsurf, Copilot Workspace — что у тебя там).
4. Пишешь ей примерно так:

> «Прочитай AI_TASKS.md в корне репозитория и выполняй задачи по порядку.  
> Начни с ЭТАПА 1. Пиши код в соответствии с описанием.»

5. А сам потом уже будешь:
   - смотреть, что она написала,
   - править архитектуру,
   - добавлять физический смысл.

Если захочешь, на следующем шаге можем:

- сделать спец-док `docs/PHYSICS_NOTES.md`,  
  где я тебе распишу **как физик** (по-человечески),  
  к чему этот код должен прийти через несколько итераций.
::contentReference[oaicite:0]{index=0}
```
